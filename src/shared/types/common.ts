// Shared lightweight types used across modules

export type RequestPriority = 'high' | 'normal' | 'low';

// Target media services supported by the UI/adapters
export type MediaService = 'sonarr' | 'radarr';
