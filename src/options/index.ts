/** Public options-domain type surface for settings and public option contracts. */
// src/options/index.ts

export type {
  BadgeVisibility,
  ExtensionOptions,
  PublicOptions,
  UiOptions,
} from './types';
